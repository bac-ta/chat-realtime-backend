spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/chat-realtime-db?useUnicode=true&characterEncoding=UTF-8&useSSL=false
    username: root
    password: Silencer&&55
    platform: mysql
    initialization-mode: always

  jpa:
    database: mysql
    show-sql: true
    hibernate:
      ddl-auto: update
      naming-strategy: org.hibernate.cfg.ImprovedNamingStrategy
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
  main:
    allow-bean-definition-overriding: true

server:
  port: 1234
  servlet:
    context-path: /api
openfire:
  secret-key: F0Q8t0rOY8hhodIe
  host: localhost
  xmpp-client-connection-port: 5222
  xmpp-domain: dimagesharevn.develop

app:
  auth:
    token-secret: ZGltYWdlc2hhcmUtbGVnYWN5LWJhY2tlbmQtcHJvamVjdA==
    token-expiration-msec: 36000000